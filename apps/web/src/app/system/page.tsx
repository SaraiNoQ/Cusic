export default function SystemPage() {
  return (
    <main className="shell">
      <section className="hero">
        <div className="eyebrow">System</div>
        <h1>Web bootstrap is wired.</h1>
        <p>
          Next step is to point this page at the API health endpoint and shared
          DTOs once dependencies are installed.
        </p>
      </section>
    </main>
  );
}

